spring:
  datasource:
    url: jdbc:postgresql://localhost:50432/securewatch
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
